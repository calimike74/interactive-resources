import VideoOverviewClient from './VideoOverviewClient';

export const metadata = {
    title: 'EQ Video Overview | Music Technology Studio',
    description: 'Watch a cinematic overview of equalisation: filter types, graphic vs parametric EQ, and Q factor explained visually.',
};

export default function VideoOverviewPage() {
    return <VideoOverviewClient />;
}
