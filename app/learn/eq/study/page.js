import StudyFlowClient from './StudyFlowClient';

export const metadata = {
    title: 'Study: EQ & Filters | Music Technology Studio',
    description: 'Build your own revision layout: drag diagrams around the text to create a personalised study sheet.',
};

export default function StudyPage() {
    return <StudyFlowClient />;
}
