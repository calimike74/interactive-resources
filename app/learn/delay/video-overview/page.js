import HaasVideoOverviewClient from './HaasVideoOverviewClient';

export const metadata = {
    title: 'Haas Effect — Video Overview | Music Technology Studio',
    description: 'Watch a short overview of the Haas (precedence) effect: fusion thresholds, the 15 dB override rule, and how engineers use micro-delays to create stereo width.',
};

export default function DelayVideoOverviewPage() {
    return <HaasVideoOverviewClient />;
}
