import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mikelehnert.interactiveresources',
  appName: 'Interactive Resources',
  webDir: 'out'
};

export default config;
